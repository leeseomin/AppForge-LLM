{
  "schema_version": "1.0",
  "summary": "Release candidate refreshed after the build.sh uv fallback hotfix. Python build, secret scan, release readiness, launcher smoke, and documentation checks passed; no deployment or publishing was performed.",
  "release_version": "0.4.0",
  "build_artifacts": [
    "dist/openappforge-0.4.0-py3-none-any.whl",
    "dist/openappforge-0.4.0.tar.gz"
  ],
  "install_and_run": [
    "Preferred source UI path: ./build.sh",
    "Prepared workspace check: APPFORGE_SKIP_INSTALL=1 APPFORGE_SKIP_FRONTEND_BUILD=1 ./build.sh --check",
    "No-browser smoke: APPFORGE_SKIP_INSTALL=1 APPFORGE_SKIP_FRONTEND_BUILD=1 APPFORGE_WEB_PORT=8799 ./build.sh --smoke",
    "Manual source path remains: python -m venv .venv; source .venv/bin/activate; python -m pip install -e '.[dev]'; npm --prefix frontend install; npm --prefix frontend run build; appforge web",
    "Wheel install path remains: python -m pip install dist/openappforge-0.4.0-py3-none-any.whl; appforge web"
  ],
  "configuration": [
    "APPFORGE_WEB_HOST defaults to 127.0.0.1.",
    "APPFORGE_WEB_PORT defaults to 8787.",
    "APPFORGE_SKIP_INSTALL=1 reuses the existing .venv.",
    "APPFORGE_SKIP_FRONTEND_BUILD=1 reuses packaged web assets.",
    "APPFORGE_DRIVER=llm-bridge or APPFORGE_START_LLM_BRIDGE=1 requests optional bridge startup; APPFORGE_SKIP_LLM_BRIDGE=1 disables launcher-owned bridge startup.",
    "APPFORGE_LLM_BRIDGE_URL defaults to http://127.0.0.1:8788."
  ],
  "migrations": [
    "No data migration is required.",
    "Existing .appforge-web runtime state remains compatible.",
    "Existing appforge CLI commands remain available."
  ],
  "release_checks": [
    {
      "check": "run_build",
      "passed": "true",
      "evidence": "appforge tool run run_build executed .venv/bin/python -m build --no-isolation and produced wheel/sdist."
    },
    {
      "check": "secret_scan",
      "passed": "true",
      "evidence": "security stage and release gate secret_scan returned no findings."
    },
    {
      "check": "release_readiness",
      "passed": "true",
      "evidence": "release_readiness reported source_present, README, gitignore, verification artifact, and security artifact all present."
    },
    {
      "check": "artifact_inventory",
      "passed": "true",
      "evidence": "artifact_inventory found dist, README.md, LICENSE, CHANGELOG.md, pyproject.toml, Python/FastAPI stack."
    },
    {
      "check": "launcher fallback and smoke",
      "passed": "true",
      "evidence": "Unsupported uv sync simulation, local uv sync failure fallback, prepared --check, and web --smoke all passed."
    },
    {
      "check": "frontend packaged assets",
      "passed": "true",
      "evidence": "npm --prefix frontend run build completed and emitted appforge/resources/web assets."
    }
  ],
  "known_issues": [
    "Online dependency audit was not run because network is disabled.",
    "SBOM generation remains lightweight/root-manifest-only and reported zero components.",
    "Fresh pip fallback without preinstalled dependencies still requires network access or a local package cache.",
    "Setuptools emits non-blocking deprecation warnings for pyproject license metadata."
  ],
  "rollback": [
    "Revert build.sh to the previous uv-sync-only dependency setup if needed.",
    "Revert README.md, docs/WEB_APP.md, and CHANGELOG.md fallback wording.",
    "Use the previous dist artifacts from Git or rebuild from the prior commit.",
    "Manual appforge web startup remains available if the launcher is bypassed."
  ]
}

# AppForge-LLM v7 릴리스 노트

릴리스 날짜: 2026-07-11

v7은 실행 중간 단계에서 간헐적으로 멈추던 브릿지·스트림 경로를 집중적으로 보강한 안정성 릴리스입니다.

## 주요 수정

1. **Bun 유휴 타임아웃 해제**  
   생성, 스트리밍, 프로바이더 테스트, 에이전트 이벤트 요청에 대해 요청별 유휴 타이머를 해제했습니다. 일반 관리 API에는 기본 30초 제한을 유지합니다.

2. **SSE 하트비트 추가**  
   기본 5초마다 keep-alive를 보내 모델의 첫 토큰이 늦거나 도구 실행 중 출력이 잠시 없어도 연결이 조용한 상태로 끊기지 않도록 했습니다.

3. **도구 결과 경쟁 조건 수정**  
   모델 스트림이 끝나기 전에 Python 러너가 도구 결과를 반환하는 경우에도 브릿지가 결과를 큐에 저장합니다. 동일 결과가 재전송되면 멱등 성공으로 처리합니다.

4. **조기 EOF 감지와 자동 연속 실행**  
   `done`, `error`, `cancelled` 없이 SSE가 끝나면 성공으로 오인하지 않고 `BRIDGE_STREAM_INTERRUPTED`로 분류합니다. 일시적 오류는 원래 단계 제한 시간 안에서 재시도하며, 에이전트 단계는 기존 파일과 이미 제출한 아티팩트를 바탕으로 이어서 진행합니다.

5. **완료 직전 연결 유실 보호**  
   필수 아티팩트와 stage-result가 이미 검증된 뒤 마지막 완료 이벤트만 사라진 경우에는 작업을 실패시키지 않습니다. 아티팩트는 모두 있지만 stage-result 제출만 빠진 경우에는 실제 변경 내역을 기반으로 복구 기록을 생성합니다.

6. **취소·연결 종료 정리**  
   브라우저나 Python 클라이언트가 스트림을 닫으면 진행 중인 공급자 요청도 함께 취소해 고아 세션과 장시간 대기를 줄였습니다.

## 새 환경 변수

```text
APPFORGE_LLM_BRIDGE_IDLE_TIMEOUT  일반 브릿지 경로의 유휴 제한(초), 기본값 30
APPFORGE_LLM_BRIDGE_HEARTBEAT_MS  SSE 하트비트 간격(ms), 기본값 5000
```

## 검증 범위

- Python 전체 테스트
- 브릿지 TypeScript 구문 검사
- Vue 타입 검사와 프로덕션 빌드
- 조기 EOF, 일시적 생성 실패, 에이전트 연속 세션, 완료 이벤트 유실 회귀 테스트

# FastAPI stack skill

Use validated request/response models and dependency injection for authentication, database sessions, and configuration. Keep domain behavior outside route functions. Define status codes and error models explicitly. Ensure authorization applies to object access, not just endpoint entry. Test with dependency overrides and a temporary database; run startup/lifespan behavior in integration tests.

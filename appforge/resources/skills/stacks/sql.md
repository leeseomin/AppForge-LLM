# SQL/data-store skill

Use parameterized queries or the framework ORM safely. Enforce critical integrity constraints in the database. Add indexes from observed access paths, not speculation. Keep migrations forward-safe and provide rollback or compensating guidance. Define transaction boundaries, isolation expectations, and concurrency behavior. Never store secrets or unbounded sensitive logs in tables.

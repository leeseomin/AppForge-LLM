# Node.js stack skill

Keep asynchronous control flow explicit, await promises, and handle process shutdown. Validate environment variables at startup. Bound request sizes, timeouts, queues, and concurrency. Do not block the event loop with CPU-heavy work. Use structured errors and stable exit codes. Keep test, lint, typecheck, and build scripts in the package manifest.

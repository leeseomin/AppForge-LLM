# Multi-tenancy

Make tenant identity explicit in authentication context and every persistent query. Enforce it in server-side data access, unique constraints, caches, jobs, file paths, and logs. Test cross-tenant access attempts. Decide whether tenant isolation is row, schema, database, or service based on risk and scale; do not claim strong isolation from a UI filter.

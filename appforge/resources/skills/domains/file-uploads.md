# File uploads

Limit size and count, validate content rather than trusting extension or MIME header, normalize names, prevent path traversal, and store uploads outside executable/static source paths. Generate server-side identifiers. Scan or quarantine risky content where required. Use signed access and authorization. Test malformed, oversized, duplicate, interrupted, and unauthorized uploads.

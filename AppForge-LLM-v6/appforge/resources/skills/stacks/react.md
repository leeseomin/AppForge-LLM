# React stack skill

Model server data, local UI state, and form state separately. Keep components focused and derive state instead of synchronizing duplicates with effects. Use semantic HTML, labels, keyboard behavior, focus management, and visible error messages. Test user-observable behavior. Avoid a global state library until shared state actually warrants it, and do not hide data fetching or authorization solely in client components.
